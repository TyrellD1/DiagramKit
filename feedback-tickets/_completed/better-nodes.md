

## Summary

Some quality of life improvements.

We may want to increate the max width of nodes by a factor of 2x but no more.

### Description

Description should not truncate AND should also also respect white space. So if I return it shows there.

We should write markdown in the description text box (this should just be native markdown) and render them on the node using a react markdown library.

Make the textarea on the editor signficantly taller -- say 3x as tall.
- make sure the edit node sidebar is scrollable.
- make the edit node sidebar wider, say 1.5x as wide.