

## Summary

Improve the edges such that on click we can have a little menu to show if they should have an arrow or just be connector. I.e if I connect two nodes, I want it clear one points to another if it's a flow vs. a 'connection'

We also must make deleting work (persist it in database)

Both the edge direction and deleting edges should persist